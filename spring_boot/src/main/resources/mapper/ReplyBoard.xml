<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.syys.mapper.ReplyBoard">

	<select id="list" resultType="com.syys.domain.ReplyVO">
		select * from reply
		where bno = #{bno}
		order by rno desc
	</select>

	<insert id="create">
		insert into reply( rno , bno, replytext, mid)
		values(reply_seq.nextval , #{bno}, #{replytext}, #{mid})
	</insert>

	<update id="update">
		update reply
		set replytext = #{replytext}, updatedate = sysdate
		where rno=#{rno}
	</update>

	<delete id="delete">
		delete from reply
		where rno=#{rno}
	</delete>
</mapper>
